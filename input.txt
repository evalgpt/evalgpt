* write a python application that has a method call(message), which takes the message and sends to openai gpt completions model
* model name and attributes like temperature etc should be environment variables
* write a pytest that tests the application call method
* write a requirements.txt file with all the app requirements
* include a Dockerfile & docker-compose that start up the application install all dependancies
* write a README.md document that has install instructions in steps
